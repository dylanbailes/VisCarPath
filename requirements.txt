# VisCarPath Project Dependencies
# Updated for depthai v2 API compatibility and OpenCV variants

# Core computer vision - choose ONE based on environment:
# For local execution (with GUI/display support via cv2.imshow()):
opencv-python>=4.5.0

# For cloud/server execution (headless, no GUI - e.g., GitHub Codespaces, Docker):
# opencv-python-headless>=4.5.0

# AprilTag detection
apriltag>=3.0.0

# DepthAI - PINNED TO V2 API (v3.0+ has breaking changes)
depthai<3.0.0

# Scientific computing
numpy>=1.20.0

# Optional: Additional utilities
scipy>=1.7.0
